<!DOCTYPE html>
<html>
  <head>
    <title>Love Spreading</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap -->
    <link href="../../css/bootstrap.css" rel="stylesheet" media="screen">
    <link href="../../css/selfdefine.css" rel="stylesheet" media="screen">
    <link href="../../css/user.css" rel="stylesheet" media="screen">
  </head>
  <body> 
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="http://127.0.0.1:3000/home">Love Spreading</a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-right">
            <li><a href="home">物品瀏覽</a></li>
            <li><a href="upload">物品捐贈</a></li>
            <% if(!user){ %>
            <li data-toggle="modal" data-target="#myModal"><a href="#">Login</a></li>
            <% } else { %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= user.name%> <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><a href="/user/logout">Logout</a></li>
              </ul>
            </li>
            <% } %>
            
            <li><a href="#">Help</a></li>
          </ul>
        </div>
      </div>
    </nav>
    <ul class="nav nav-tabs">
      <li class="active">
<!--         <a href="#" id="trade">自由交易</a>
      </li>
      <li><a href="#" id="charity">公益特區</a></li> -->
    </ul>
    <div class="container-fluid">
      <!--Content and sidebar-->
      <div class="row row-offcanvas row-offcanvas-left">
        <div class="col-sm-3 col-md-2 sidebar-offcanvas" id="sidebar" role="navigation">
          <br>

        </div>
        <form role="form" name="dealform" method="POST" action="/deal">

        <div class="col-sm-9 col-md-10 main content">
          <!--Body content-->
        <% items.forEach( function ( item ){ %>
        <% if(item.supply_id){ %>
          <div class="col-sm-4">
            <img class="item_img_size" src="../../img/closet.jpg">
          </div>
          
          <div class="col-sm-4">
            <div class="item_holder">supply_id：<%= item.supply_id %></div>
            <div class="item_holder">_id：<%= item._id %></div>
            <div class="item_holder">商品名稱：<%= item.item_name %></div>
            <div class="item_holder">上架人：<%= item.supplier_name %></div>
            <div class="credit">單價：<%= item.credit %></div>
            <div class="credit">數量：<%= item.amount %></div>
            <div class="item_description">商品描述<br><%= item.description %></div>
          </div>
          
          <div class="col-sm-4">
              <div class="item_holder">交換資訊</div>
              <input type="text" class="form-control input-md hidden"  id="supply_id" name= "supply_id" value="<%= item.supply_id %>">
              <input type="text" class="form-control input-md hidden"  id="item_name" name= "item_name" value="<%= item.item_name %>">
              <input type="text" class="form-control input-md hidden"  id="supplier_name" name= "supplier_name" value="<%= item.supplier_name %>">
              <input type="text" class="form-control input-md hidden"  id="supplier_name" name= "supplier_id" value="<%= item.supplier_id %>">
              <input type="text" class="form-control input-md hidden"  id="credit" name= "credit" value="<%= item.credit %>">
                <% if(!user){ %>
                <input type="text" class="form-control input-md" placeholder="阿呆" id="name" name="name"><br>
                <%} else{ %>
                <input type="text" class="form-control input-md hidden" id="name" name="name" value="<%= user.id%>"><br>
                <% } %>
                <label>數量  </label>
                <input type="text" class="form-control input-md"  id="amount" name= "amount"><br>
                <% if(!item.face){ %>
                <p>&nbsp&nbsp&nbsp&nbsp面交：賣家不提供面交服務</p><br>
                <%} else{ %>
                <input type="radio" name="method" value="face">  面交地點：<%= item.face %> <br>
                <% } %>            
                <% if(!item.delivery){ %>
                <p>&nbsp&nbsp&nbsp&nbsp郵寄：賣家不提供郵寄服務</p><br>
                <%} else{ %>
                <input type="radio" name="method" value="delivery">  買家自負郵資：<%= item.delivery %> <br>
                <% } %>
                <input type="checkbox" name="res_check" value="checked">我了解Love Spreading不涉及任何金錢交易責任 <br><br>
                <button class="btn buy_btn"  type="submit" onclick="return(confirm("are you sure?"))">我要交換</button>
          </div>
        </form>

        <%} else{ %>
          <div class="col-sm-4">
            <img class="item_img_size" src="../../img/closet.jpg">
          </div>
          
          <div class="col-sm-5">
          <div class="item_holder">need_id：<%= item.need_id %></div>
          <div class="item_holder">_id：<%= item._id %></div>
          <div class="item_holder">商品名稱：<%= item.item_name %></div>
          <div class="item_holder">上架人：<%= item.needer_name %></div>
          <div class="credit">需求數量：<%= item.amount %></div>
          <div class="item_description">商品描述<br><%= item.description %></div>
          <p>如有意捐贈，請透過以下方式聯繫公益團體</p>
          <% if(item.tel){ %>
            <div class="tel">連絡電話: <%= item.tel %></div>
            <%} else{} %>
          <% if(item.email){ %>
            <div class="email">電子郵件: <%= item.email %></div>
            <%} else{} %>
        <% } %>
      <% }); %>
      </div>
    <!-- Modal -->
    <div class="modal fade" id="dealModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
      <div class="modal-dialog modal-sm">
        <div class="modal-content">
          <div class="modal-header">
            <h1 class="text-center">Attention</h1>
          </div>
          <div class="modal-body">
            <p>It will cost you credits.</p>
            <p>Are you sure to exchange this item?</p>
          </div>
          <div class="modal-footer">
            <div class="col-md-12">
              <button class="btn btn-info">Confirm</button>
              <button class="btn" data-dismiss="modal" aria-hidden="true">Cancel</button>
            </div>  
          </div>
        </div>
      </div>
    </div>
    <!-- modal end -->    
  </body>
    <script src="http://code.jquery.com/jquery.js"></script>
    <script src="../../js/bootstrap.js"></script>
    <script src="../../js/home.js"></script>

</html>